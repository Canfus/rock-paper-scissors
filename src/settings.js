export const SETTINGS = {
  /**
   * Represents the maximum limit for each element's count.
   * @constant {number}
   */
  EACH_MAX: 20,
  /**
   * Represents the background color used in the canvas.
   * @constant {string}
   */
  BACKGROUND_COLOR: "#fff",
  /**
   * A constant array that represents the speed in two dimensions.
   * Each element in the array corresponds to a specific axis of movement.
   * Typically used to define or manipulate speed-related calculations or properties.
   * @constant {[number, number]}
   */
  SPEED: [3, 3],
  /**
   * Represents the size of an element.
   * This constant is used to define a fixed size value for operations or calculations
   * involving elements.
   * @constant {number}
   */
  ELEMENT_SIZE: 24,
};
