import { SETTINGS } from "../settings.js";

export class Settings {
  static MAX_ELEMENTS = SETTINGS.EACH_MAX;
  static BACKGROUND_COLOR = SETTINGS.BACKGROUND_COLOR;
  static SPEED = SETTINGS.SPEED;
  static SIZE = SETTINGS.ELEMENT_SIZE;
}
